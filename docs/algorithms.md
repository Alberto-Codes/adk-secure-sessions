# Algorithm Documentation

adk-secure-sessions delegates all cryptographic operations to the
[pyca/cryptography](https://cryptography.io/) library. No custom cryptographic
primitives are implemented. This page documents the algorithms, parameters, and
standards compliance for each registered encryption backend.

For how encrypted data is framed and identified, see the
[Envelope Protocol](envelope-protocol.md).

## Algorithm Overview

The current release ships with one backend: **Fernet** (backend ID `0x01`).

Fernet provides authenticated encryption by combining AES-128-CBC for
confidentiality with HMAC-SHA256 for integrity. Key material can be a
pre-generated Fernet key or an arbitrary passphrase derived via
PBKDF2-HMAC-SHA256.

Fernet keys are 256 bits (32 bytes), composed of two 128-bit halves:

- **Bytes 0 -- 15:** HMAC-SHA256 signing key (message authentication)
- **Bytes 16 -- 31:** AES-128-CBC encryption key (confidentiality)

This explains why the algorithm is called "AES-128-CBC" despite the overall
key being 32 bytes — the 32 bytes are split, with only the second half used
for AES encryption.

## NIST/FIPS Compliance Mapping

Every cryptographic component maps to a published NIST or FIPS standard:

| Component | Algorithm | Standard | Reference |
|-----------|-----------|----------|-----------|
| Block cipher | AES-128-CBC | FIPS 197 (AES) + NIST SP 800-38A (CBC mode) | 128-bit key, CBC mode of operation |
| Message authentication | HMAC-SHA256 | FIPS 198-1 | Keyed-hash message authentication code |
| Key derivation | PBKDF2-HMAC-SHA256 | NIST SP 800-132 | Password-based key derivation |
| Initialization vector | 128-bit random IV | NIST SP 800-38A | Per-message, generated by Fernet |

!!! note "FIPS 140-2 Certification"
    This library uses NIST-approved algorithms via pyca/cryptography, which
    builds on OpenSSL. FIPS 140-2 certification applies to the underlying
    cryptographic module (OpenSSL), not to libraries built on top of it.
    Organizations requiring FIPS 140-2 compliance should verify their
    deployment's OpenSSL build is FIPS-validated.

## Component Details

### AES-128-CBC (Confidentiality)

Advanced Encryption Standard with 128-bit keys in Cipher Block Chaining mode.
Each message is encrypted with a fresh 128-bit random IV (initialization
vector), generated by the Fernet implementation. Plaintext is PKCS7-padded to
16-byte block boundaries before encryption.

### HMAC-SHA256 (Integrity)

Hash-based Message Authentication Code using SHA-256. Applied over the Fernet
token's version, timestamp, IV, and ciphertext to detect tampering. The signing
key (first 128 bits of the Fernet key) is independent of the encryption key.

### PBKDF2-HMAC-SHA256 (Key Derivation)

Password-Based Key Derivation Function 2 using HMAC-SHA256 as the
pseudorandom function. Used when the caller provides an arbitrary passphrase
instead of a pre-generated Fernet key.

**Current parameters:**

| Parameter | Value | Source |
|-----------|-------|--------|
| Hash function | SHA-256 | `hashlib.pbkdf2_hmac("sha256", ...)` |
| Iterations | `480,000` | `_PBKDF2_ITERATIONS` in `backends/fernet.py` |
| Salt | `b"adk-secure-sessions-fernet-v1"` | `_PBKDF2_SALT` in `backends/fernet.py` |
| Output length | 32 bytes (256 bits) | Fernet key requirement |

### Key Resolution

`FernetBackend` accepts keys as `str` or `bytes` and resolves them as follows:

1. **Valid Fernet key** — if the input is a valid base64url-encoded 32-byte
   key, it is used directly (passthrough).
2. **Arbitrary passphrase** — otherwise, the input is derived into a valid
   Fernet key via PBKDF2-HMAC-SHA256 with the parameters above.

## Fernet Token Format

The ciphertext portion of the envelope (bytes 2+) contains a Fernet token. A
Fernet token is a versioned, timestamped, authenticated ciphertext with the
following internal structure: version byte (`0x80`), 8-byte big-endian
timestamp, 16-byte random IV, AES-128-CBC ciphertext padded to 16-byte blocks,
and a 32-byte HMAC-SHA256 authentication tag. See the
[Fernet Specification](https://github.com/fernet/spec/blob/master/Spec.md)
for the canonical format definition.

## Async Implementation

All `FernetBackend` encrypt and decrypt operations are wrapped in
`asyncio.to_thread()` because Fernet's underlying AES and HMAC computations
are CPU-bound. This prevents blocking the async event loop in ADK agent
pipelines.

## Known Limitations

**Fixed salt.** The PBKDF2 salt (`b"adk-secure-sessions-fernet-v1"`) is
application-scoped, not per-key. Identical passphrases always produce the same
derived key. Phase 3 (Story 3.2) will add per-key random salt to eliminate this
property.

**PBKDF2 iteration count.** The current iteration count of 480,000 meets the
NIST SP 800-132 baseline, which recommends an iteration count "as large as
verification server performance will allow." Current OWASP guidance (2023
Password Storage Cheat Sheet) recommends 600,000 iterations for
PBKDF2-HMAC-SHA256. Phase 3 will increase the count to meet current OWASP
guidance.

**No FIPS 140-2 certification.** The library uses NIST-approved algorithms but
FIPS 140-2 certification belongs to the underlying cryptographic module
(OpenSSL), not the application library. A FIPS deployment guide is planned for
Phase 4.

## Related

- [Envelope Protocol](envelope-protocol.md) — binary envelope format wrapping the ciphertext
- [Roadmap](ROADMAP.md) — Phase 3 improvements (AES-256-GCM, per-key salt, iteration increase)
- [FernetBackend API Reference](reference/adk_secure_sessions/backends/fernet.md) — full API documentation
